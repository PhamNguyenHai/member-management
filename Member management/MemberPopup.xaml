<Window x:Class="Member_management.MemberPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Member_management"
        mc:Ignorable="d"
        Title="MemberPopup" Height="430" Width="400" 
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Border CornerRadius="10" Background="White" BorderBrush="#CCC" BorderThickness="1" Padding="15">
            <StackPanel>
                <TextBlock Text="Add New Member" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="#333" />

                <TextBlock Text="Number:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtNumber" Margin="0,0,0,10" Style="{StaticResource filterTextBox}"/>

                <TextBlock Text="Name:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtName" Margin="0,0,0,10" Style="{StaticResource filterTextBox}"/>

                <TextBlock Text="Position:" FontWeight="SemiBold"/>
                <ComboBox x:Name="cbbPosition" Margin="0,0,0,10" Style="{StaticResource comboboxFilter}" ItemsSource="{Binding Positions}" DisplayMemberPath="Name" SelectedValuePath="Id" />


                <TextBlock Text="Email:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtEmail" Margin="0,0,0,10" Style="{StaticResource filterTextBox}"/>

                <TextBlock Text="Phone:" FontWeight="SemiBold"/>
                <TextBox x:Name="txtPhoneNumber" Margin="0,0,0,10" Style="{StaticResource filterTextBox}"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Style="{StaticResource addButton}" Margin="0 0 6 0" Background="Green" Width="80" Click="Button_Click">
                        <TextBlock Text="Submit"/>
                    </Button>
                    <Button x:Name="btnCancel" Style="{StaticResource addButton}" Margin="0" Background="Gray" Width="80" Click="btnCancel_Click">
                        <TextBlock Text="Cancel"/>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</Window>
